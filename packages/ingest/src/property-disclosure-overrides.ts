export type PropertyDisclosureOverride = {
  disclosureName: string;
  fileSeq?: number;
  effectiveDate?: string;
  memberId: string;
};

export const propertyDisclosureOverrides: PropertyDisclosureOverride[] = [];
